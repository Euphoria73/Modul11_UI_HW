<Window x:Class="Modul11_UI_HW.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Modul11_UI_HW"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <HierarchicalDataTemplate
            x:Key="Organization"
            DataType="{x:Type local:Department}"
            ItemsSource="{Binding Departaments}">
            <TextBlock Text="{Binding Name}" />
        </HierarchicalDataTemplate>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top" >
            <MenuItem Header ="Открыть" Command ="{Binding OpenCommand}"/>
            <MenuItem Header ="Сохранить" Command="{Binding SaveCommand}"/>
        </Menu>
        
        <Grid>
            <TreeView x:Name="TV_Structure" Margin="0,10,576,10"
                       ItemTemplate="{StaticResource Organization}"
                        ItemsSource="{Binding MyOrganization, UpdateSourceTrigger=PropertyChanged}"
                      >
                <TreeView>
                    <HierarchicalDataTemplate ItemsSource="{Binding Path=Departments}">
                        <TextBlock Text="{Binding NameDepartment}"/>
                    </HierarchicalDataTemplate>
                </TreeView>
            </TreeView>
        </Grid>
    </DockPanel>
</Window>
